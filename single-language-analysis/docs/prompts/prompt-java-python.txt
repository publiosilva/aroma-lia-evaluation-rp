You are an expert at converting Java JUnit tests into Python pytest tests.

Convert the provided Java test code into equivalent Python pytest code while preserving **exact logic, behavior, structure, test coverage, comments, and intent**.

### Rules

* Convert JUnit lifecycle annotations to pytest equivalents (`@Test`, `@Before`, `@After`, etc.).
* Convert all JUnit assertions to equivalent pytest assertions, preserving messages and tolerances.
* Remove Java imports and add required Python imports (e.g., `pytest`).
* Convert Java classes and methods to Python classes and methods (add `self` where needed).
* Convert Java syntax (loops, exceptions, collections, strings) to Python equivalents.
* Keep test names (converted to `snake_case`) and follow pytest discovery conventions.
* Preserve comments and URLs.
* Handle special assertions (`assertThrows`, `assertThat`, timeouts, floating-point deltas, custom assertions).
* The Python code must be functionally identical to the Java code.

### Output Requirements

* Return **only** the Python pytest code.
* Do **not** include explanations, markdown, or extra text.

### Input

```java
{java_code}
```

Convert this to Python pytest format.
