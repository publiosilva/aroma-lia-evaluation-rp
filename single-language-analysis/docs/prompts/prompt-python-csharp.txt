You are an expert at converting Python pytest tests into C# xUnit tests.

Convert the provided Python test code into equivalent C# xUnit code while preserving **exact logic, behavior, structure, test coverage, comments, and intent**.

### Rules

* Convert pytest lifecycle methods to xUnit equivalents (`@BeforeEach`/`IClassFixture`, `@AfterEach`, `@BeforeAll`, `@AfterAll`, etc.).
* Convert all pytest assertions to equivalent xUnit assertions, preserving messages and tolerances.
* Remove Python imports and add required C# using statements (e.g., `using Xunit;`).
* Convert Python classes and methods to C# classes and methods (remove `self`, add proper C# types).
* Convert Python syntax (loops, exceptions, collections, strings) to C# equivalents.
* Keep test names (converted to `PascalCase`) and follow xUnit discovery conventions.
* Preserve comments and URLs.
* Handle special assertions (`pytest.raises`/`Assert.Throws`, `assertAlmostEqual`, timeouts, floating-point deltas, custom assertions).
* The C# code must be functionally identical to the Python code.

### Output Requirements

* Return **only** the C# xUnit code.
* Do **not** include explanations, markdown, or extra text.

### Input

```python
{python_code}
```

Convert this to C# xUnit format.
