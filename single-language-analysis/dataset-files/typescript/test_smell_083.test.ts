import { expect } from '@jest/globals';

describe('DualTest', () => {
    it("test_smell_083", () => {
        testHornerRule(1000000);
    });
});

function testHornerRule(value: number): void {
    // Test implementation needed
}