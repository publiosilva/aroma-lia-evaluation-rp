const { expect } = require('jest');

describe('Sourcetypes', () => {
  it.skip("test_smell_163", () => {
    const paramsReq = [argFile];
    const paramsOpt = [];
    comm.validateArgs(paramsReq, paramsOpt, args);
    os.system(`classify "${args[argFile]}"`);
  });
});