const { expect } = require('jest');

describe('GeneratorTest', () => {
    it("test_smell_037", () => {
        // empty
        // expect(generator.alphabet.getAlphabet()).toBe(PasswordOptions.UPPERCASE_LETTERS);
    });
});